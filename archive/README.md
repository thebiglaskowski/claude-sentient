# Archive

This directory is reserved for historical code that is no longer actively used.

The V1 legacy implementation was removed in v0.5.1 to reduce repository size (~310 files, 3.4MB). It is preserved in git history for reference.
